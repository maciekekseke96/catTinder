import React from 'react';
import './FinalOptions.scss';

const FinalOptions = () => {
  return <div className="finalOptions">
      <button className="button restart">Restart with same category</button>
      <button className='button new'>Select new Category</button>
  </div>;
};

export default FinalOptions;
